{% block content %}
    <div class="container w-full grid grid-cols-2 gap-10">
        <h3 class="text-2xl font-bold">{{ fields.text }}</h3>
        <img src="{{ fields.image.url }}" />
    </div>
{% endblock %}